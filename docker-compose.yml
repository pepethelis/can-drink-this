services:
  app:
    build:
      context: .
      args:
        PUBLIC_SITE_URL: http://localhost:8080
    image: can-drink-this
    ports:
      - "8080:8080"
    environment:
      PUBLIC_SITE_URL: http://localhost:8080
    restart: unless-stopped
