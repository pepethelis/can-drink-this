filters:
  and:
    - file.folder == "content/reviews/own"
views:
  - type: table
    name: Таблиця
    filters:
      and:
        - brand == "exstase"
    order:
      - file.name
      - createdAt
    sort:
      - property: file.mtime
        direction: DESC
