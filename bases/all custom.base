views:
  - type: table
    name: own
    filters:
      and:
        - file.folder == "content/reviews/own"
    order:
      - file.name
      - related
    sort:
      - property: file.size
        direction: DESC
  - type: table
    name: subscribers
    filters:
      and:
        - file.folder == "content/reviews/subscribers"
  - type: table
    name: friendly
    filters:
      and:
        - file.folder == "content/reviews/friendly"
  - type: table
    name: all
    filters:
      and:
        - file.folder.startsWith("content/reviews")
