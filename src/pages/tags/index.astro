---
import { getCollection } from "astro:content";
import Main from "@/layouts/Main.astro";
import Layout from "@/layouts/Layout.astro";
import Tag from "@/components/Tag.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import getUniqueTags from "@/utils/getUniqueTags";
import { SITE } from "@/config";
import postFilter from "@/utils/postFilter";
import { PAGE_META, ogImageFor } from "@/pageMeta";

const posts = await getCollection("reviews", ({ data }) =>
  postFilter({ data })
);

let tags = getUniqueTags(posts);
---

<Layout
  title={`${PAGE_META.tags.title} | ${SITE.title}`}
  description={PAGE_META.tags.description}
  ogImage={ogImageFor("tags")}
>
  <Header />
  <Main pageTitle="Tags" pageDesc="All the tags used in posts.">
    <ul class="flex flex-wrap gap-6">
      {tags.map(({ tag, tagName }) => <Tag {tag} {tagName} />)}
    </ul>
  </Main>
  <Footer />
</Layout>
